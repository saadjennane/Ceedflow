node_modules
dist
# Toute base locale, y compris une copie mise de côté après un incident.
.pgdata*
*.log
.DS_Store

# Cache de compilation TypeScript : régénéré à chaque build, il salissait
# chaque commit d'une ligne qui ne dit rien.
*.tsbuildinfo
